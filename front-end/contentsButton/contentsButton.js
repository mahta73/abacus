import React from 'react';
import { StyleSheet, View, Text} from 'react-native';

export default function ContentsButton(props) {
    return (
        <Text>
            {props.title}
        </Text>
    )
}